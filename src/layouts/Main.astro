---
import SiteTopbar from "@/components/SiteTopbar.astro";
import { SITE } from "@/config";

type Props = {
  pageTitle?: string;
  pageDesc?: string;
  pageEyebrow?: string;
};

const { pageTitle, pageDesc, pageEyebrow } = Astro.props;

const backUrl = SITE.showBackButton ? Astro.url.pathname : "/";
---

<SiteTopbar />
<div>
  <main
    data-backUrl={backUrl}
    data-post-memory-scope="list"
    id="main-content"
    class="app-layout pb-12 lg:max-w-5xl"
  >
    <div class="px-1 py-4 sm:px-2 sm:py-5">
      {
        pageTitle && (
          <header class="max-w-5xl mb-2">
            {
              pageEyebrow && (
                <p class="text-[0.76rem] font-medium tracking-[0.2em] text-foreground/36 uppercase">
                  {pageEyebrow}
                </p>
              )
            }
            <h1 class="text-lg font-medium tracking-tight text-foreground">
              {pageTitle}
            </h1>
          </header>
        )
      }
      {pageDesc && (<p class="mt-4 max-w-3xl text-[1rem] leading-8 text-foreground/60">{pageDesc}</p>)}
      <slot />
    </div>
  </main>
</div>

<script>
  document.addEventListener("astro:page-load", () => {
    const mainContent: HTMLElement | null =
      document.querySelector("#main-content");
    const backUrl = mainContent?.dataset?.backurl;
    if (backUrl) {
      sessionStorage.setItem("backUrl", backUrl);
    }
  });
</script>
