---
import SiteTopbar from "@/components/SiteTopbar.astro";
import ArtalkComments from "@/components/ArtalkComments.astro";
import PageHeader from "@/components/PageHeader.astro";
import CodeSnippet from "@/components/CodeSnippet.astro";
import Layout from "@/layouts/Layout.astro";
import { defaultFriendAvatar, friendLinks, siteProfile } from "@/data/friends";
import { SITE } from "@/config";
import { ui } from "@/i18n";

const locale = "en";
const copy = ui[locale];
const applicationFields = [
  { label: "name", value: "miuo's blog" },
  { label: "url", value: siteProfile.href },
  { label: "avatar", value: siteProfile.avatar },
  { label: "desc", value: siteProfile.description },
];
const applicationText = applicationFields
  .map(field => `${field.label}: ${field.value}`)
  .join("\n");
---

<Layout
  title={`${copy.pages.friendsTitle} | ${SITE.title}`}
  description={copy.pages.friendsDesc}
  structuredDataType="CollectionPage"
  structuredData={{
    mainEntity: {
      "@type": "ItemList",
      name: copy.pages.friendsTitle,
      numberOfItems: friendLinks.length,
    },
  }}
  {locale}
>
  <SiteTopbar />

  <main id="main-content" class="mx-auto w-full max-w-4xl px-4 sm:px-6 pt-6 pb-10 md:pt-10 md:pb-14 overflow-x-hidden">
    <PageHeader title={copy.pages.friendsTitle} />
    <section class="py-4" data-pagefind-body>
      <div class="max-w-4xl">
        <section>
          {
            friendLinks.length > 0 ? (
              <div class="grid grid-cols-1 gap-3 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 sm:gap-4">
                {friendLinks.map(friend => (
                  <a
                    href={friend.href}
                    target="_blank"
                    rel="noreferrer"
                    class="group flex items-center gap-3 rounded-xl border border-gray-100/60 dark:border-gray-800/60 p-3.5 text-left transition-all duration-300 ease-out hover:-translate-y-1 hover:bg-gray-50/60 dark:hover:bg-gray-800/30 hover:shadow-[0_8px_30px_rgba(0,0,0,0.06)] dark:hover:shadow-[0_8px_30px_rgba(0,0,0,0.2)] active:scale-[0.99] sm:flex-col sm:items-center sm:gap-0 sm:p-4 sm:text-center"
                  >
                    <img
                      src={friend.avatar ?? defaultFriendAvatar}
                      alt={`${friend.name} avatar`}
                      class="h-12 w-12 shrink-0 rounded-full object-cover ring-1 ring-border/30 transition-transform duration-500 ease-out group-hover:scale-105 sm:mb-2.5"
                      loading="lazy"
                    />
                    <div class="min-w-0 sm:w-full">
                      <h2 class="line-clamp-1 text-sm font-semibold leading-snug text-foreground transition-colors duration-300 group-hover:text-accent">{friend.blog ?? friend.name}</h2>
                      {
                        friend.blog && friend.blog !== friend.name && (
                          <p class="mt-0.5 text-[0.7rem] leading-tight text-foreground/40">{friend.name}</p>
                        )
                      }
                      <p class="mt-1 line-clamp-2 text-xs leading-snug text-foreground/40">{friend.description}</p>
                    </div>
                  </a>
                ))}
              </div>
            ) : (
              <div class="border-y border-border/65 py-8">
                <p class="text-[1.05rem] leading-8 text-foreground/68">
                  No friend links yet. Exchanged links will be listed here.
                </p>
              </div>
            )
          }
        </section>

        <section class="apply-section">
          <div>
            <p class="section-eyebrow">Apply</p>
            <h2>Friend Link Format</h2>
            <p>
              Leave a comment below in this format to exchange links. Existing
              friends can also comment if their information needs an update.
            </p>
          </div>

          <div class="friend-application mt-6">
            <p class="friend-application-title">Template</p>
            <CodeSnippet code={applicationText} language="yaml" />
          </div>
        </section>
      </div>

      <div class="max-w-4xl">
        <ArtalkComments pageKey="/en/friends" pageTitle={copy.pages.friendsTitle} />
      </div>
    </section>
  </main>

</Layout>

<style>
  .apply-section {
    margin-top: 2.6rem;
    border-top: 1px solid color-mix(in srgb, var(--border) 68%, transparent);
    padding-top: 2.6rem;
  }

  .section-eyebrow {
    color: color-mix(in srgb, var(--foreground) 36%, transparent);
    font-size: 0.76rem;
    font-weight: 500;
    letter-spacing: 0.32em;
    text-transform: uppercase;
  }

  .apply-section h2 {
    margin-top: 0.75rem;
    font-size: 1.85rem;
    font-weight: 600;
    line-height: 1.18;
  }

  .apply-section p:not(.section-eyebrow) {
    max-width: 42rem;
    margin-top: 1rem;
    color: color-mix(in srgb, var(--foreground) 66%, transparent);
    font-size: 1rem;
    line-height: 2;
  }

  .friend-application {
    position: relative;
  }

  .friend-application-title {
    margin: 0 0 0.5rem;
    color: color-mix(in srgb, var(--foreground) 72%, transparent);
    font-size: 0.9rem;
    font-weight: 600;
  }


  @media (max-width: 639px) {
    .apply-section {
      margin-top: 2rem;
      padding-top: 2rem;
    }

    .apply-section h2 {
      font-size: 1.55rem;
    }

    .friend-application-title {
      font-size: 0.82rem;
    }
  }
</style>
