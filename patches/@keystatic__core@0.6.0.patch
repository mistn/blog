diff --git a/dist/keystatic-core-api-generic.js b/dist/keystatic-core-api-generic.js
index aabdb1a49688d48a681ca4e06fb68ee7ca1e0869..ca48ad347ae783a8c7aacd9c4b13276652ac39ed 100644
--- a/dist/keystatic-core-api-generic.js
+++ b/dist/keystatic-core-api-generic.js
@@ -218,6 +218,7 @@ async function githubOauthCallback(req, config) {
   url.searchParams.set('client_id', config.clientId);
   url.searchParams.set('client_secret', config.clientSecret);
   url.searchParams.set('code', code);
+  url.searchParams.set('redirect_uri', `${new URL(req.url).origin}/api/keystatic/github/oauth/callback`);
   const tokenRes = await fetch(url, {
     method: 'POST',
     headers: {
@@ -225,6 +226,8 @@ async function githubOauthCallback(req, config) {
     }
   });
   if (!tokenRes.ok) {
+    const ghBody = await tokenRes.text();
+    console.error('GitHub token exchange failed:', tokenRes.status, ghBody);
     return {
       status: 401,
       body: 'Authorization failed'
@@ -235,6 +238,7 @@ async function githubOauthCallback(req, config) {
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
+    console.error('GitHub token schema validation failed:', JSON.stringify(_tokenData));
     return {
       status: 401,
       body: 'Authorization failed'
